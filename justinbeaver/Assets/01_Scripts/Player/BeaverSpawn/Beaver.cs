using UnityEngine;

public class Beaver : MonoBehaviour
{
    public static Beaver Instance;

    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
